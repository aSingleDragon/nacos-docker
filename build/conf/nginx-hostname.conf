events {
    worker_connections  1024;
}

http {

    upstream nacos-cluster {
        server  nacos-1:8848;
        server  nacos-2:8848;
        server  nacos-3:8848;
    }

    server {
        listen       80;
        location / {
            proxy_pass http://nacos-cluster;
        }
    }
}

